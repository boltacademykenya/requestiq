import { InfoCallout, InfoCta, InfoPageShell, ArticleSection } from '@/components/info-page-shell'

const steps = [['01', 'Create your workspace', 'Create an organization account and invite the teammates who will act on reorder opportunities.'], ['02', 'Connect your data', 'Import customer and order history with the fields your team already uses to understand repeat behavior.'], ['03', 'Review opportunities', 'Use reorder likelihood, timing, customer value, and purchase history to prioritize outreach.'], ['04', 'Take action', 'Contact customers through your existing workflows and measure repeat revenue over time.']]

export default function DocumentationPage() { return <InfoPageShell eyebrow="Resources" title="Documentation" description="Learn how to set up ReorderIQ, understand predictions, and turn customer order history into timely repeat-revenue actions."><div className="grid gap-3 sm:grid-cols-2">{['Quickstart', 'Data & imports', 'Reorder predictions', 'Workspace guide'].map((item, index) => <a key={item} href={`#section-${index}`} className="surface p-5 transition hover:shadow-lift"><p className="text-sm font-semibold text-emerald-700">0{index + 1}</p><p className="mt-3 font-semibold">{item}</p><p className="mt-1 text-sm text-slate-500">Read the essentials for this area.</p></a>)}</div><ArticleSection title="Quickstart"><p>ReorderIQ works best when your workspace includes enough historical order information to show customer cadence. Start by creating your organization account, then connect or import customer and order data.</p><div className="grid gap-3 md:grid-cols-4">{steps.map(([number, title, copy]) => <div key={number} className="surface p-4"><span className="flex size-8 items-center justify-center rounded-full bg-emerald-500/10 text-xs font-bold text-emerald-800">{number}</span><h3 className="mt-4 text-sm font-semibold">{title}</h3><p className="mt-2 text-xs leading-5 text-slate-500">{copy}</p></div>)}</div></ArticleSection><ArticleSection title="Understanding reorder predictions"><p>A reorder prediction is a prioritization signal based on observed purchase behavior. It combines recency, frequency, average order value, and a customer's typical reorder cycle to help your team decide who to contact next.</p><InfoCallout title="Use predictions as a guide">The highest likelihood customer is not always the only customer worth contacting. Combine the score with customer value, product availability, and your team's relationship context.</InfoCallout></ArticleSection><ArticleSection title="Recommended data"><p>For useful predictions, provide a stable customer identifier, order date, order value, and product or category information. Consistent identifiers allow ReorderIQ to connect multiple orders to the same customer.</p><p>Only connect data your organization is authorized to use. Keep your source records accurate and remove duplicate or test orders where possible.</p></ArticleSection><ArticleSection title="Workspace basics"><p>The dashboard surfaces customers who may be approaching their next purchase, estimated opportunity value, and repeat-rate trends. Use customer detail views to understand the history behind each recommendation before acting.</p></ArticleSection><InfoCta /></InfoPageShell> }
