'use client'

import Link from 'next/link'
import { useState } from 'react'
import { ArrowRight, Check, ChevronDown, CreditCard, FileText, LockKeyhole, MessageCircle, X } from 'lucide-react'
import { faqs, formatKes, getPlan, pricingPlans, type PlanId } from '@/lib/pricing'

function PricingCard({ plan, onSelect }: { plan: typeof pricingPlans[number]; onSelect: (id: PlanId) => void }) {
  return <article className={`relative flex flex-col rounded-3xl bg-white p-6 ${plan.highlighted ? 'shadow-lift' : 'shadow-soft'}`}>
    {plan.highlighted && <span className="absolute -top-3 left-6 rounded-full bg-emerald-700 px-3 py-1 text-[11px] font-bold text-white shadow-sm shadow-emerald-900/25">Most Popular</span>}
    <h2 className="text-lg font-semibold text-slate-950">{plan.name}</h2><p className="mt-3 min-h-12 text-sm leading-6 text-slate-500">{plan.description}</p>
    <div className="mt-6 min-h-14">{plan.price ? <><span className="text-3xl font-bold tracking-tight text-slate-950">{plan.priceLabel}</span><span className="ml-1 text-sm text-slate-500">/ month</span></> : <span className="text-3xl font-bold tracking-tight text-slate-950">{plan.priceLabel}</span>}</div>
    <button onClick={() => onSelect(plan.id)} className={`mt-6 inline-flex h-11 items-center justify-center rounded-xl px-4 text-sm font-semibold transition ${plan.highlighted ? 'bg-emerald-700 text-white shadow-sm shadow-emerald-900/20 hover:bg-emerald-800' : 'bg-wash text-slate-700 hover:bg-wash-strong hover:text-slate-900'}`}>{plan.cta}</button>
    <ul className="mt-6 space-y-3 text-sm text-slate-600">{plan.features.map((feature) => <li key={feature} className="flex gap-2"><Check size={16} className="mt-0.5 shrink-0 text-emerald-600" />{feature}</li>)}</ul>
  </article>
}

function CustomPricingForm({ onClose }: { onClose: () => void }) {
  const [submitted, setSubmitted] = useState(false)
  if (submitted) return <div className="p-7 text-center"><div className="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-emerald-50 text-emerald-700"><Check /></div><h2 className="mt-4 text-xl font-semibold">Thanks. We&apos;ve received your request.</h2><p className="mt-2 text-sm text-slate-500">Our team will be in touch about a plan built around your business.</p><button onClick={onClose} className="mt-6 rounded-lg bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white">Close</button></div>
  return <form onSubmit={(event) => { event.preventDefault(); setSubmitted(true) }} className="grid gap-4 p-6 sm:grid-cols-2"><div className="sm:col-span-2"><h2 className="text-xl font-semibold">Talk to Sales</h2><p className="mt-1 text-sm text-slate-500">Tell us what your team needs and we&apos;ll shape the right ReorderIQ plan.</p></div>{['Full Name', 'Business Name', 'Email', 'Phone Number', 'Business Size', 'Number of Customers', 'Approximate Monthly Orders', 'Current Ecommerce/CRM Platform'].map((label) => <label key={label} className="text-xs font-semibold text-slate-600">{label}<input required className="field mt-1 h-10 w-full px-3.5 text-sm font-normal" /></label>)}<label className="sm:col-span-2 text-xs font-semibold text-slate-600">Required integrations<textarea className="field mt-1 min-h-20 w-full p-3.5 text-sm font-normal" /></label><label className="sm:col-span-2 text-xs font-semibold text-slate-600">Message<textarea className="field mt-1 min-h-20 w-full p-3.5 text-sm font-normal" /></label><button className="sm:col-span-2 rounded-lg bg-emerald-700 px-4 py-3 text-sm font-semibold text-white hover:bg-emerald-800">Request Custom Pricing</button></form>
}

function CheckoutModal({ initialPlan, onClose }: { initialPlan: PlanId; onClose: () => void }) {
  const [planId, setPlanId] = useState<PlanId>(initialPlan); const [step, setStep] = useState<'details' | 'payment' | 'processing' | 'success' | 'failed' | 'cancelled'>('details'); const [method, setMethod] = useState<'M-Pesa' | 'Paystack'>('M-Pesa'); const plan = getPlan(planId)
  if (step === 'success') return <div className="p-8 text-center"><div className="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-emerald-50 text-emerald-700"><Check /></div><h2 className="mt-4 text-2xl font-semibold">You&apos;re all set!</h2><p className="mt-2 text-sm text-slate-500">Your ReorderIQ subscription is now active.</p><div className="mx-auto mt-6 max-w-xs rounded-xl bg-slate-50 p-4 text-left text-sm"><p>Plan: <b>{plan.name}</b></p><p className="mt-2">Amount: <b>{plan.priceLabel} / month</b></p><p className="mt-2">Next billing date: <b>30 days from activation</b></p></div><Link href="/workspace" className="mt-6 inline-flex rounded-lg bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white">Go to Dashboard</Link></div>
  if (step === 'failed' || step === 'cancelled') return <div className="p-8 text-center"><h2 className="text-2xl font-semibold">{step === 'failed' ? 'Payment unsuccessful' : 'Payment cancelled'}</h2><p className="mt-2 text-sm text-slate-500">{step === 'failed' ? "We couldn't complete your payment." : 'Your subscription has not been activated.'}</p><div className="mt-6 flex justify-center gap-3"><button onClick={() => setStep('payment')} className="rounded-xl bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white shadow-sm shadow-emerald-900/20">{step === 'failed' ? 'Try Again' : 'Return to Payment'}</button><button onClick={onClose} className="rounded-xl bg-wash px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-wash-strong">Return to Pricing</button></div></div>
  if (step === 'processing') return <div className="p-12 text-center"><div className="mx-auto h-8 w-8 animate-spin rounded-full border-2 border-emerald-200 border-t-emerald-700" /><h2 className="mt-5 text-xl font-semibold">Processing your payment...</h2><p className="mt-2 text-sm text-slate-500">Your subscription will only activate after secure provider confirmation.</p></div>
  return <div className="max-h-[90vh] overflow-y-auto p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-wider text-emerald-700">Secure subscription setup</p><h2 className="mt-1 text-xl font-semibold">Complete your subscription</h2></div><button onClick={onClose} aria-label="Close" className="rounded-lg p-2 text-slate-400 hover:bg-slate-100"><X size={18} /></button></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.1fr_.9fr]">{step === 'details' ? <form onSubmit={(event) => { event.preventDefault(); setStep('payment') }} className="grid gap-4 sm:grid-cols-2"><p className="sm:col-span-2 text-sm font-semibold">Business information</p>{['Business name', 'Full name', 'Email', 'Phone number', 'Country'].map((label) => <label key={label} className="text-xs font-semibold text-slate-600">{label}<input required className="field mt-1 h-10 w-full px-3.5 text-sm font-normal" /></label>)}<label className="text-xs font-semibold text-slate-600">Business size<select className="field mt-1 h-10 w-full px-3.5 text-sm font-normal"><option>Small business</option><option>Medium business</option><option>Large business</option></select></label><button className="sm:col-span-2 rounded-lg bg-emerald-700 px-4 py-3 text-sm font-semibold text-white">Continue to Payment</button></form> : <div><p className="text-sm font-semibold">Pay with:</p><div className="mt-3 grid gap-3 sm:grid-cols-2"><button onClick={() => setMethod('M-Pesa')} aria-pressed={method === 'M-Pesa'} className={`rounded-2xl p-4 text-left transition ${method === 'M-Pesa' ? 'bg-emerald-500/15 shadow-soft' : 'bg-wash hover:bg-wash-strong'}`}><MessageCircle className="text-emerald-700" size={18} /><p className="mt-3 text-sm font-semibold">M-Pesa</p><p className="mt-1 text-xs text-slate-500">STK Push</p></button><button onClick={() => setMethod('Paystack')} aria-pressed={method === 'Paystack'} className={`rounded-2xl p-4 text-left transition ${method === 'Paystack' ? 'bg-emerald-500/15 shadow-soft' : 'bg-wash hover:bg-wash-strong'}`}><CreditCard className="text-emerald-700" size={18} /><p className="mt-3 text-sm font-semibold">Paystack</p><p className="mt-1 text-xs text-slate-500">Secure Checkout</p></button></div><button onClick={() => { setStep('processing'); setTimeout(() => setStep('success'), 900) }} className="mt-6 w-full rounded-xl bg-emerald-700 px-4 py-3 text-sm font-semibold text-white shadow-sm shadow-emerald-900/20">Continue with {method}</button><button onClick={() => setStep('cancelled')} className="mt-3 w-full text-sm text-slate-500">Cancel payment</button></div>}<aside className="well p-5"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Selected plan</p><select value={planId} onChange={(event) => setPlanId(event.target.value as PlanId)} className="field mt-3 h-10 w-full px-3.5 text-sm font-semibold"><option value="small">Small Business</option><option value="medium">Medium Business</option><option value="large">Large Business</option></select><p className="mt-5 text-2xl font-bold">{plan.priceLabel}<span className="ml-1 text-sm font-normal text-slate-500">/ month</span></p><div className="mt-5 flex gap-2 text-xs text-slate-500"><LockKeyhole size={14} className="text-emerald-700" /> Payment credentials are handled by the provider.</div></aside></div></div>
}

export default function PricingPage() {
 const [selectedPlan, setSelectedPlan] = useState<PlanId | null>(null); const [customOpen, setCustomOpen] = useState(false)
 const selectPlan = (id: PlanId) => id === 'custom' ? setCustomOpen(true) : setSelectedPlan(id)
 return <main className="min-h-screen bg-canvas text-slate-950"><header className="sticky top-0 z-40 bg-canvas/85 shadow-header backdrop-blur-md"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8"><Link href="/" className="flex items-center gap-2 text-lg font-bold tracking-tight"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-emerald-700 text-sm text-white">R</span>ReorderIQ</Link><nav className="hidden items-center gap-6 text-sm text-slate-600 md:flex"><Link href="/">Product</Link><a href="#faq">FAQ</a><Link href="/sign-in">Log in</Link></nav><Link href="/sign-up" className="rounded-lg bg-emerald-700 px-3 py-2 text-xs font-semibold text-white">Get Started</Link></div></header><section className="mx-auto max-w-7xl px-5 pb-12 pt-16 text-center lg:px-8 lg:pt-24"><p className="text-sm font-semibold text-emerald-700">Pricing that grows with your repeat revenue</p><h1 className="mx-auto mt-3 max-w-3xl text-4xl font-bold tracking-[-0.04em] sm:text-6xl">Simple pricing. More repeat revenue.</h1><p className="mx-auto mt-5 max-w-2xl text-base leading-7 text-slate-600">Choose a ReorderIQ plan that fits your business. Start turning customer purchase patterns into actionable reorder opportunities.</p><p className="mt-5 text-sm font-semibold text-slate-500">All plans are billed monthly.</p></section><section className="mx-auto grid max-w-7xl gap-4 px-5 pb-20 lg:grid-cols-4 lg:px-8">{pricingPlans.map((plan) => <PricingCard key={plan.id} plan={plan} onSelect={selectPlan} />)}</section><section id="faq" className="mx-auto max-w-3xl px-5 pb-20 lg:px-8"><div className="text-center"><p className="text-sm font-semibold text-emerald-700">Questions, answered</p><h2 className="mt-2 text-3xl font-bold tracking-tight">Pricing FAQ</h2></div><div className="mt-8 space-y-3">{faqs.map(([question, answer]) => <details key={question} className="surface group px-5 py-4"><summary className="flex cursor-pointer list-none items-center justify-between text-sm font-semibold"><span>{question}</span><ChevronDown size={17} className="transition group-open:rotate-180" /></summary><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">{answer}</p></details>)}</div></section><section className="bg-slate-950 px-5 py-20 text-center text-white"><p className="text-sm font-semibold text-emerald-300">Ready to turn existing customers into repeat revenue?</p><h2 className="mx-auto mt-3 max-w-2xl text-3xl font-bold tracking-tight">Start identifying your next reorder opportunities.</h2><div className="mt-7 flex flex-col justify-center gap-3 sm:flex-row"><Link href="/sign-up" className="rounded-lg bg-emerald-500 px-5 py-3 text-sm font-semibold text-slate-950">Start ReorderIQ</Link></div></section><footer className="bg-white"><div className="mx-auto flex max-w-7xl flex-col gap-3 px-5 py-7 text-xs text-slate-500 sm:flex-row sm:items-center sm:justify-between lg:px-8"><Link href="/" className="font-semibold text-slate-800">ReorderIQ</Link><span>© 2026 ReorderIQ. All rights reserved.</span></div></footer>{selectedPlan && <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/40 p-0 sm:items-center sm:p-4"><div className="w-full max-w-3xl rounded-t-3xl bg-white shadow-lift sm:rounded-3xl"><CheckoutModal initialPlan={selectedPlan} onClose={() => setSelectedPlan(null)} /></div></div>}{customOpen && <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/40 p-0 sm:items-center sm:p-4"><div className="max-h-[92vh] w-full max-w-3xl overflow-y-auto rounded-t-3xl bg-white shadow-lift sm:rounded-3xl"><div className="flex justify-end px-5 pt-5"><button onClick={() => setCustomOpen(false)} aria-label="Close" className="rounded-lg p-2 text-slate-400 hover:bg-slate-100"><X size={18} /></button></div><CustomPricingForm onClose={() => setCustomOpen(false)} /></div></div>}</main>
}
